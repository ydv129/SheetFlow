# 🏗️ MASTER_PROMPT.md: SheetFlow AI Implementation & Audit Protocol

**Role:** Senior Full-Stack Architect & Open-Source Maintainer.
**Project:** SheetFlow AI — A local-first, zero-cloud Excel dashboarding and AI analysis tool built for MSMEs.
**Stack:** Next.js 15, React 19, Tailwind CSS, shadcn/ui, SheetJS, WebLLM (Gemma-4), MongoDB Atlas, **Valkey** (In-Memory Data Store).

---

## 🛑 STRICT EXECUTION RULES (ZERO DEVIATION)
Before writing any code, acknowledge and strictly enforce these rules:

1. **No Placeholders:** NEVER write `// TODO: implement logic`. Write fully functioning, complete, and copy-pasteable code blocks.
2. **Human-Readable & Starter-Friendly:** Write code that a junior open-source contributor can easily read. Use descriptive variable names (`parsedExcelRows` instead of `dataX`). 
3. **Double-Moderated Optimization:** Every function must pass two internal checks:
   - *Review 1 (Performance):* Are we preventing unnecessary React re-renders? 
   - *Review 2 (Security):* Are API routes protected? Is data leaking?
4. **No AI Comment Clutter:** Do not over-comment obvious syntax. Scrub all "Generated by AI" or meta-commentary from the final code.
5. **Strict Separation of Concerns:** Frontend UI and Backend Logic must reside in explicitly isolated namespaces.

---

## 📂 FOLDER STRUCTURE (MONOREPO STYLE)
Maintain this strict separation to ensure the backend and frontend are logically isolated.

/
├── /frontend          (ALL Client-Side UI & Components)
│   ├── /components    (UI primitives like shadcn, Charts, Tables)
│   ├── /hooks         (Custom React hooks like useExcelLink, useLocalAI)
│   └── /store         (Local state management)
├── /backend           (ALL Server-Side Logic & APIs)
│   ├── /api           (Clean, RESTful Next.js Route Handlers)
│   ├── /auth          (NextAuth.js configurations)
│   ├── /middleware    (Security, Rate Limiting via Valkey)
│   └── /db            (MongoDB & Valkey connection logic)
├── /lib               (Shared TypeScript types, constants)
├── /docs              (Open Source documentation)
└── /.github           (GitHub Actions, Issue/PR Templates)

---

## 🔐 PHASE 1: Auth, Security & REST APIs
1. **Authentication:** Implement `Auth.js (NextAuth)` with Google OAuth. 
2. **Security First (Middleware & Valkey):** Write a strict `middleware.ts`. Integrate **Valkey** as the in-memory data store for strict API rate-limiting and session caching to prevent abuse.
3. **MongoDB Atlas (Metadata):** Create a Mongoose schema for `User` and `ProjectConfig`. 
   - *Strict Rule:* NEVER store actual spreadsheet row data in the database.
4. **Human-Designed REST APIs:** All endpoints in `/backend/api` must follow strict RESTful conventions (`GET`, `POST`, `PATCH`, `DELETE`) with standard status codes and JSON formats.

---

## 🔌 PHASE 2: The Excel Live-Link
1. **File System Access API:** Create a custom hook `useExcelLiveLink.ts`. Save the `FileSystemFileHandle` in `IndexedDB`.
2. **The Watcher:** Implement a polling interval (every 5 seconds) inside a Web Worker to check `file.lastModified`.
3. **Parsing:** Use `SheetJS (xlsx)` to convert the binary buffer to JSON instantly in the browser. 

---

## 🧠 PHASE 3: Local-First AI Engine
1. **WebLLM Integration:** Create a `useLocalAI.ts` hook. Initialize the WebGPU engine using `@mlc-ai/web-llm`.
2. **Tiered Strategy:** Default to `SmolLM2-360M`, trigger `Gemma-4-E2B` only for complex analysis.
3. **Zero-Cloud Guarantee:** Enforce that no fetch requests containing Excel JSON data are ever sent to the backend.

---

## 📊 PHASE 4: UI/UX & Dashboards
1. **Layout Engine:** Build a 3-pane responsive layout: Command Bar, KPI Cards, Main Data Table.
2. **Data Table:** Implement `@tanstack/react-table` with `@tanstack/react-virtual`. Must handle 10,000+ rows seamlessly.
3. **Dynamic Visuals:** Integrate `Recharts` to swap the Table for a Chart automatically for trend data.

---

## 🚀 PHASE 5: Open-Source Readiness
1. **next.config.js:** Optimize for production (image domains, strict mode, SWC minification).
2. **Documentation Drafting:** Draft phase-specific docs, including `.env.example` with MongoDB and Valkey URL placeholders.

---

## 🧹 PHASE 6: Automation, Polish & Git Push
1. **Consolidate Documentation:** Merge all docs into `README.md` and `ARCHITECTURE.md`.
2. **NPM Automation:** Update `package.json` to include unified scripts (e.g., `npm run dev:all`).
3. **Code Scrubbing:** Perform a global search and destroy for LLM placeholder comments. 
4. **GitHub Standardization:** Create `.github/workflows/ci.yml` for strict linting.
5. **Final Push:** Provide the exact sequence of `git add`, `git commit` (semantic message), and `git push` commands.

---

## 🔍 THE ARCHITECT'S FINAL AUDIT CHECKLIST
Execute a line-by-line audit before finalizing code:

- [ ] **No Placeholders:** Zero instances of `// TODO` or "Insert logic here".
- [ ] **Virtualization Verified:** Verify `@tanstack/react-virtual` is correctly applied to the table.
- [ ] **Re-render Prevention:** Verify `useMemo` and `useCallback` are applied to table logic.
- [ ] **Valkey Integration:** Ensure Valkey is properly configured as a drop-in replacement for rate limiting and edge caching, completely avoiding proprietary Redis lock-ins.
- [ ] **Data Zero-Knowledge:** Audit every API route. Verify NO Excel row data is passed to the Vercel backend.
- [ ] **Clean UI/UX:** Ensure loading states use UI Skeletons, not basic spinners. Remove all robotic text phrasing.
- [ ] **Artifact Destruction:** Delete all temporary `.txt` or `.md` files used during development.

---
**Execution Command:** Begin implementation sequentially. 
**Step 1:** Output the complete code for Phase 1 (Folder scaffolding, MongoDB/Valkey Setup, NextAuth, and REST APIs). 
Do not proceed to Phase 2 until Step 1 is fully coded and robust.